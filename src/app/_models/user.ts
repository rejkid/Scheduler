
import { Role } from './role';
import { Schedule } from './schedule';

export class User {
    id: string;
    firstName: string;
    secondName: string;
    email: string;
    function: string;
    date: Date;
}